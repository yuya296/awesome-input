{
  "name": "awesome-input",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf out",
    "build": "npm run clean && tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "test:e2e": "npm run build && playwright test tests/extension.fixture.e2e.spec.js",
    "test:e2e:live": "npm run build && playwright test tests/live",
    "test:e2e:all": "npm run build && playwright test tests/extension.fixture.e2e.spec.js && playwright test tests/live"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "typescript": "^6.0.2"
  }
}
